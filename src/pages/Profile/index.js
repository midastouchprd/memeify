import React from 'react';
import Logout from '../../components/Logout';

const Profile = () => {
  return (
    <div>
      <h1>Profile</h1>
      <Logout />
    </div>
  );
};

export default Profile;
